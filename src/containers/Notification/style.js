import { StyleSheet } from "react-native";
import { Colors, Metrics, Fonts } from "../../theme";

export default StyleSheet.create({
    acceptAndDeclineText: {
        color: "#FFFFFF",
        fontSize: 12,
        // fontStyle: Fonts.type.Arial
    }
})